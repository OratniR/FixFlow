# Backend Environment Variables

# Database Connection (Postgres)
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://postgres:postgres@db:5432/fixflow

# Embedding Configuration
# Set to 'true' to use local SentenceTransformers (CPU friendly, no API key needed)
# Set to 'false' to use OpenAI (requires OPENAI_API_KEY)
USE_LOCAL_EMBEDDING=true

# OpenAI API Key (Only needed if USE_LOCAL_EMBEDDING=false)
# OPENAI_API_KEY=sk-...
